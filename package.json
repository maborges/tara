{
  "name": "balanca-platform",
  "private": true,
  "scripts": {
    "station:dev": "pnpm --dir station dev",
    "station:build": "pnpm --dir station build",
    "station:package": "pnpm --dir station package:offline",
    "station:typecheck": "pnpm --dir station typecheck",
    "station:lint": "pnpm --dir station lint",
    "backoffice:dev": "pnpm --dir backoffice dev",
    "backoffice:build": "pnpm --dir backoffice build",
    "backoffice:typecheck": "pnpm --dir backoffice typecheck",
    "backoffice:lint": "pnpm --dir backoffice lint",
    "portal:dev": "pnpm --dir portal dev",
    "portal:build": "pnpm --dir portal build",
    "portal:typecheck": "pnpm --dir portal typecheck",
    "portal:lint": "pnpm --dir portal lint"
  },
  "devDependencies": {
    "@eslint/js": "9.39.5",
    "@typescript-eslint/parser": "8.68.0",
    "globals": "14.0.0"
  }
}
